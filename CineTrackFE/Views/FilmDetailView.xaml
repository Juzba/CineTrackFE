<UserControl
    x:Class="CineTrackFE.Views.FilmDetailView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:CineTrackFE.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CineTrackFE.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:CineTrackFE.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:FilmDetailViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    FontSize="16"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:ImagePathConverter x:Key="ImagePathC" />
    </UserControl.Resources>

    <Grid Background="#3D5B81">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="55*" />
            <ColumnDefinition Width="45*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="70*" />
            <RowDefinition Height="30*" />
        </Grid.RowDefinitions>



        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            VerticalAlignment="Bottom"
            FontSize="16"
            Foreground="Red"
            Text="{Binding ErrorMessage}"
            TextAlignment="Center"
            TextWrapping="Wrap" />

        <StackPanel Grid.Column="1" Margin="20,0">
            <TextBlock
                FontSize="35"
                FontWeight="Bold"
                Foreground="#DFFBFC"
                Text="{Binding Film.Name}" />
            <TextBlock Margin="0,10" Text="{Binding Film.Director}" />
            <TextBlock Margin="0,10" Text="{Binding Film.ReleaseDate, StringFormat={}{0:dd. MM. yyyy}}" />
            <TextBlock
                Margin="0,10"
                Text="{Binding Film.Description}"
                TextWrapping="Wrap" />

            <ItemsControl Margin="0,10" ItemsSource="{Binding Film.Genres}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Margin="0,0,15,0"
                            BorderBrush="#DFFBFC"
                            BorderThickness="1"
                            CornerRadius="2">
                            <TextBlock
                                Padding="10,5"
                                FontSize="16"
                                Foreground="#DFFBFC"
                                Text="{Binding}" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>



        <Image
            Grid.RowSpan="2"
            Grid.Column="2"
            HorizontalAlignment="Left"
            Source="{Binding Film.ImageFileName, Converter={StaticResource ImagePathC}}">
            <Image.Effect>
                <DropShadowEffect
                    BlurRadius="60"
                    ShadowDepth="20"
                    Color="Black" />
            </Image.Effect>
        </Image>






        <TextBlock
            Grid.Row="1"
            Grid.Column="1"
            Grid.ColumnSpan="2"
            Margin="-5"
            Padding="30"
            VerticalAlignment="Bottom"
            FontSize="16"
            FontWeight="DemiBold"
            Foreground="White"
            Text="{Binding ErrorMessage}"
            TextAlignment="Center"
            TextWrapping="Wrap" />


    </Grid>








</UserControl>
