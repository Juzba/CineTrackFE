<UserControl
    x:Class="CineTrackFE.Views.FilmView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CineTrackFE.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:CineTrackFE.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:FilmViewModel}"
    d:DesignHeight="720"
    d:DesignWidth="1280"
    prism:ViewModelLocator.AutoWireViewModel="True"
    FontSize="25"
    mc:Ignorable="d">



    <Grid Background="#3D5B81">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" MinHeight="60" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70*" />
            <ColumnDefinition Width="25*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition />
            </Grid.RowDefinitions>







        </Grid>


        <TextBlock
            FontSize="20"
            Foreground="Red"
            Text="{Binding ErrorMessage}"
            TextAlignment="Center" />



        <DataGrid
            Grid.Row="1"
            Margin="20,0"
            HorizontalAlignment="Center"
            VerticalAlignment="Top"
            AutoGenerateColumns="False"
            Background="#293241"
            CanUserAddRows="false"
            FontSize="17"
            Foreground="#DFFBFC"
            IsReadOnly="True"
            ItemsSource="{Binding FilmList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
            SelectionMode="Single"
            SelectionUnit="Cell">
            <DataGrid.Columns>
                <DataGridTextColumn
                    MinWidth="150"
                    Binding="{Binding Name}"
                    Header="Title" />
                <DataGridTextColumn
                    MinWidth="150"
                    Binding="{Binding Director}"
                    Header="Director" />
                <DataGridTextColumn
                    MinWidth="150"
                    Binding="{Binding Year}"
                    Header="Year" />


            </DataGrid.Columns>
        </DataGrid>
    </Grid>




</UserControl>
