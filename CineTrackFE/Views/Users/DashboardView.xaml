<UserControl
    x:Class="CineTrackFE.Views.Users.DashboardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:convertors="clr-namespace:CineTrackFE.Common.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CineTrackFE.Views.Users"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:viewmodels="clr-namespace:CineTrackFE.ViewModels.Users"
    d:DataContext="{d:DesignInstance Type=viewmodels:DashboardViewModel}"
    d:DesignHeight="546"
    d:DesignWidth="975"
    prism:ViewModelLocator.AutoWireViewModel="True"
    FontSize="25"
    mc:Ignorable="d">

    <UserControl.Resources>
        <convertors:ImagePathConverter x:Key="ImagePathC" />
    </UserControl.Resources>


    <Grid Background="#3D5B81">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>



        <TextBlock
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="3"
            VerticalAlignment="Bottom"
            FontSize="16"
            Foreground="Red"
            Text="{Binding ErrorMessage}"
            TextAlignment="Center"
            TextWrapping="Wrap" />



        <ScrollViewer Grid.Column="1">
            <StackPanel>
                <TextBlock
                    Margin="0,10"
                    FontStyle="Oblique"
                    FontWeight="Bold"
                    Text="Latest Movies" />

                <ItemsControl Margin="0,0,0,50" ItemsSource="{Binding LatestsFilmList}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                            <Button
                                Height="auto"
                                Command="{Binding DataContext.OpenFilmDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <StackPanel>

                                            <Border
                                                Width="140"
                                                Height="200"
                                                Margin="10,0,10,5"
                                                BorderBrush="Black"
                                                BorderThickness="0.3"
                                                CornerRadius="5">
                                                <Image
                                                    Margin="5"
                                                    Source="{Binding ImageFileName, Converter={StaticResource ImagePathC}}"
                                                    Stretch="UniformToFill" />

                                            </Border>
                                            <TextBlock
                                                FontSize="14"
                                                FontWeight="ExtraBlack"
                                                Foreground="#293241"
                                                Text="{Binding Name}"
                                                TextAlignment="Center" />
                                            <TextBlock
                                                FontSize="14"
                                                Foreground="#DFFBFC"
                                                Text="{Binding ReleaseDate, StringFormat={}{0: d. M. yyyy}}"
                                                TextAlignment="Center" />
                                        </StackPanel>

                                    </ControlTemplate>
                                </Button.Template>
                            </Button>


                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>


                <TextBlock
                    Margin="0,10"
                    FontStyle="Oblique"
                    FontWeight="Bold"
                    Text="Favorite Films" />


                <ItemsControl ItemsSource="{Binding FavoriteFilms}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                            <Button
                                Height="auto"
                                Margin="0,0,0,20"
                                Command="{Binding DataContext.OpenFilmDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Cursor="Hand">
                                <Button.Template>
                                    <ControlTemplate TargetType="Button">
                                        <StackPanel>

                                            <Border
                                                Width="115"
                                                Height="150"
                                                Margin="10,0,10,5"
                                                BorderBrush="Black"
                                                BorderThickness="0.3"
                                                CornerRadius="5">
                                                <Image
                                                    Margin="5"
                                                    Source="{Binding ImageFileName, Converter={StaticResource ImagePathC}}"
                                                    Stretch="UniformToFill" />

                                            </Border>
                                            <TextBlock
                                                FontSize="10"
                                                Foreground="#DFFBFC"
                                                Text="{Binding Name}"
                                                TextAlignment="Center" />

                                        </StackPanel>

                                    </ControlTemplate>
                                </Button.Template>
                            </Button>


                        </DataTemplate>
                    </ItemsControl.ItemTemplate>

                </ItemsControl>


            </StackPanel>
        </ScrollViewer>


    </Grid>




</UserControl>
